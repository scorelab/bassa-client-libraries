dist: trusty
language: python
python:
  - "3.5"

cache: pip

sudo: required

services:
  - docker

notifications:
  webhooks: https://www.travisbuddy.com/

script:
  - sudo chmod -R a+rwx $TRAVIS_BUILD_DIR
  - bash run_tests.sh
